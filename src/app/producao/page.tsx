import { EmBreve } from "@/app/_components/em-breve";

export default function ProducaoPage() {
  return (
    <EmBreve
      titulo="Produção"
      descricao="Produzir lote: baixa o BOM, gera as unidades e os QRs (operação atômica)."
    />
  );
}
