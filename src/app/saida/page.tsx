import { EmBreve } from "@/app/_components/em-breve";

export default function SaidaPage() {
  return (
    <EmBreve
      titulo="Saída"
      descricao="Bipagem do QR na saída do kit: baixa, registra destino e recusa baixa dupla."
    />
  );
}
